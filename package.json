{
  "name": "ai-lesson-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based AI Lesson Planner for Netlify",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0"
  }
}